# Accept radius of the circle
radius = raw_input("Enter radius : ")

#Convert radius from string to float
radius = float(radius)

'''
Now to find area we use the given formula
area = pi * radius * radius
'''
area = 3.14 * radius * radius
print "Area is " + str(area)
